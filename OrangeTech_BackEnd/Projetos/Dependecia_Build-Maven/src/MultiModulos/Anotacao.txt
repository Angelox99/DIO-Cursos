(Exemplos de projetos(4 projetos diferentes))
Core
Service
Webapp
Controller

(Exemplo de Multimodulo(os 4 projetos fazem parte de 1 so))
    Core
    Service
    Webapp
    Controller

Plugins mais utilizados:
    -Eclipse
    -Jacoco
    -ear
    -war
    -compile
    -clean
    -checkstyle
    -javadoc

    USO
        mvn (plugin-name):(goal-name)